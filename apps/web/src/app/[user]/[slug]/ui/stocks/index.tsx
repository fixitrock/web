export function Stocks({
    params,
    searchParams,
}: {
    params: { user: string }
    searchParams: Promise<{ [key: string]: string | string[] | undefined }>
}) {
    return <div>Stocks</div>
}
